# ==================================
# Development Profile Configuration
# ==================================
spring.config.activate.on-profile=dev

# ==================================
# Dev Database (MySQL)
# ==================================
spring.datasource.url=jdbc:mysql://localhost:3306/playlist_db?serverTimezone=Europe/Tirane&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==================================
# DevTools and Cache
# ==================================
spring.devtools.livereload.enabled=false
spring.thymeleaf.cache=false
spring.web.resources.chain.cache=false

# ==================================
# JWT (same as base)
# ==================================
app.jwt-secret=akarmhJexsqj1gsBXOgND3wasqf2a03e/kR7ok2Ayco=
app.jwt-expiration-milliseconds=86400000

# ==================================
# CORS
# ==================================
auth.allowed-origins=*
auth.whitelist=/api/auth/login,/api/auth/register,/

# The physical location on your server's disk where files will be saved
upload.path=./uploads

# You can also configure the max file size
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Path to the FFmpeg executable
ffmpeg.path=/usr/bin/ffmpeg

# Path to the FFprobe executable
ffprobe.path=/usr/bin/ffprobe

# --- EMAIL CONFIGURATION ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=debora.mezini17@gmail.com
spring.mail.password=yrij uqzi dklr xwkc

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

frontend.base-url=http://192.168.2.109:5173



