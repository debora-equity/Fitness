# ==================================
# Base Application Settings (Default)
# ==================================
spring.application.name=playlist
server.port=8285

# ==================================
# JPA / Hibernate Settings (Base)
# ==================================
# Use 'update' for development to keep schema and data persistent between runs.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- BREVO SMTP CONFIGURATION ---
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=9bc7fe001@smtp-brevo.com
spring.mail.password=vW4LIgAQszM6ph2P
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
app.mail.from=fitnessap@outlook.com
app.mail.from-name=FitnessApp

spring.servlet.multipart.max-file-size=10240MB
spring.servlet.multipart.max-request-size=10240MB

# ==================================
# Profiles
# ==================================
# Activate 'dev' profile automatically when no other profile is specified
spring.profiles.active=dev

# ==================================
# JWT Configuration
# ==================================
# Use a strong, Base64-encoded secret (replace with your real one!)
app.jwt-secret=akarmhJexsqj1gsBXOgND3wasqf2a03e/kR7ok2Ayco=
app.jwt-expiration-milliseconds=15552000000

# ==================================
# Security & CORS
# ==================================
auth.allowed-origins=http://localhost:5174,http://127.0.0.1:5174,http://192.168.2.109:5174,https://vioapp.al
auth.whitelist=/api/auth/login,/api/auth/register,/


# compression
server.compression.enabled=true
server.compression.mime-types=application/pdf,application/json,application/xml,text/html,text/plain
server.compression.min-response-size=1024


